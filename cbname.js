x = [
    7,
    36,
    13
  ];

O = Math.floor(2147483648 * Math.random()).toString(x[1])
            + Math.abs(Math.floor(2147483648 * Math.random()) ^ gen(45)).toString(x[1]);

function gen(D, Q, n, J, M, O, x) {
    if (!((D >> 1) % ((((x = [
        'Start and end points must be the same length',
        3,
        'Start and end parameters must be arrays'
    ], D) + 5) % 4 || (this.B = !0, this.I = n, this.S = Q, this.G = null), (D + x[1]) % 12) || (O = Date.now()), x[1]))) {
        if (Tc.call(this), !Array.isArray(Q) || !Array.isArray(n))
        throw Error(x[2]);
        if (Q.length != n.length)
        throw Error(x[0]);
        (this.duration = (this.W = (this.progress = 0, this.L = null, M), J), this).I = Q, (this.coords = [], this).o = n;
    }
    return O;
}